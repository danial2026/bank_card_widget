name: bank_card_widget_example
description: Example app demonstrating the bank_card_widget package.
publish_to: 'none'
version: 0.0.2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bank_card_widget:
    path: ../
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true 
